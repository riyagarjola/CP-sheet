#include<bits/stdc++.h>
using namespace std;
 
int main(){
int n,k,l,c,d,p,nl,np;
cin >> n >> k >> l >>c >> d >> p >> nl >> np;
 
int total_drink=k*l/nl;
int lime_toast=c*d;
int salt_toast = p/np;
 
cout<<min({total_drink,lime_toast,salt_toast})/n;
return 0;
}
